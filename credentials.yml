# credentials.yml for BizFlow AI
# This file contains the credentials for the messaging channels
# that your BizFlow AI assistant uses.
# For more details, see: https://rasa.com/docs/rasa/messaging-and-voice-channels

rest:
  # The REST channel requires no credentials by default.

# Uncomment and configure additional channels if needed for your enterprise deployment:
# facebook:
#   verify: "<verify>"
#   secret: "<your secret>"
#   page-access-token: "<your page access token>"

# slack:
#   slack_token: "<your slack token>"
#   slack_channel: "<the slack channel>"
#   slack_signing_secret: "<your slack signing secret>"

# socketio:
#   user_message_evt: <event name for user message>
#   bot_message_evt: <event name for bot messages>
#   session_persistence: <true/false>

# mattermost:
#   url: "https://<mattermost instance>/api/v4"
#   token: "<bot token>"
#   webhook_url: "<callback URL>"

# Credentials for the Rasa Enterprise channel (if using Rasa Enterprise).
rasa:
  url: "http://localhost:5002/api"
